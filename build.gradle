
apply plugin: 'com.github.dcendents.android-maven'
//apply plugin: 'com.novoda.bintray-release'

Properties properties = new Properties()
properties.load(project.rootProject.file('local.properties').newDataInputStream())
String pubVersion = properties.getProperty('publish.version')

// 생성되는 파일 이름
group = 'com.github.GDmail91'
version = pubVersion
logger.quiet(pubVersion)
/*
// 업로드용
publish {
    userOrg = 'onycom-inc'
    groupId = 'io.imqa'
    artifactId = 'imqa-mpm-injector'
    publishVersion = pubVersion
    desc = 'injector for imqa-sdk'
    website = 'https://github.com/onycom-imqa/IMQA-MPM-Injector'
    issueTracker = "${website}/issues"
    repository = "${website}.git"
}*/


buildscript {
    repositories {
        jcenter()
        maven { url "https://jitpack.io" }
    }
    dependencies {
        classpath 'com.github.dcendents:android-maven-gradle-plugin:1.5'
    }
}
/*

dependencies {
    implementation 'com.github.GDmail91:LibraryJitpack:1.0'
}
*/

allprojects {
    repositories {
        maven { url 'https://jitpack.io' }
    }
}